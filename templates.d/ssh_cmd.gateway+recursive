#!/bin/sh
echo "ssh -l `cat $SCRIPTPATH/vpngw_user` -i $SCRIPTPATH/key -p `cat $SCRIPTPATH/vpngw_port` `cat $SCRIPTPATH/vpngw_host` -C ssh -l root -i `cat $SCRIPTPATH/vpngw_keydir`/$MACHINE.pk $MACHINE -C $@"
