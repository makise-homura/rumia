#!/bin/sh
echo "ssh -o ConnectTimeout=10 -l `cat $SCRIPTPATH/vpngw_user` -i $SCRIPTPATH/key -p `cat $SCRIPTPATH/vpngw_port` `cat $SCRIPTPATH/vpngw_host` -C ssh -o ConnectTimeout=10 -l root -i `cat $SCRIPTPATH/vpngw_keydir`/$MACHINE.pk $MACHINE -C $@"
